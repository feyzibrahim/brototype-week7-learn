<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
    <%- include("./partials/nav.ejs") %>
    <div class="blogs content">
      <h2>All Blogs</h2>
      <% if(blogs.length > 0) { blogs.forEach(blog => { %>
      <a href="/blogs:" target="_blank" rel="noopener noreferrer"></a>
      <% }) %> <% } else { %>
      <p>No message here</p>
      <% } %>
    </div>
    <%- include("./partials/footer.ejs") %>
  </body>
</html>
